import React from 'react'

function props(props) {
    // const{name}=props
  return (
    <div>
        
      <h1 className='text-2xl'>{props.name}</h1>
    </div>
  )
}

export default props
